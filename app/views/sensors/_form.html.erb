<%= form_for(@sensor) do |f| %>
  <% if @sensor.errors.any? %>
    <div id="error_explanation">
        <h3><%= pluralize(@sensor.errors.count, "error") %> prohibited this sensor from being saved:</h3>
      <ul>
      <% @sensor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
        <label class="col-sm-2 control-label"><%= f.label :name %></label>
        <%= f.text_field :name %>
    </div>
    <div class="form-group">
      <label class="col-sm-2 control-label"><%= f.label :description %></label>
      <%= f.text_field :description %>
    </div>
    <div class="form-group">
      <label class="col-sm-2 control-label"><%= f.label :model %></label>
      <%= f.text_field :model %>
    </div>
    <div class="form-group actions">
      <%= f.submit 'Submit', :class => 'btn btn-lg btn-success btn-block' %>
    </div>
<% end %>
