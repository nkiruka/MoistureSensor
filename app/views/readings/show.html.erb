<table>
  <tr>
    <th>Sensor ID</th>
    <th>Value</th>
    <th>Created At</th>
  </tr>
  <% @readings.each do |reading| %>
  <tr>
    <td><%= "#{@reading.sensor.id}" %></td>
    <td><%= "#{@reading.value}" %></td>
    <td><%= "#{@reading.created_at}" %></td>
  </tr>
  <% end %>
</table>
